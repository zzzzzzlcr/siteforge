# 第十二轮：有界重探（控制器裁定）+ 完整三遍 —— **被用户叫停**

**状态**：STOPPED_BY_USER —— 代码做完、钉子绿；**完整三遍没跑完**（用户中途说
「别重试了，可以了，资料不准先算了」→ 立刻收手，不再起新的验收跑）
**commit**：`52f5534`（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **406 passed / 12 skipped**（新增 2 条钉子；旧断言一条没放宽）

## 1. 做了什么（控制器的裁定：有界重探）

**裁定照抄**：探索结束若 `_explore_reached_success` 为 False → **自动重探，最多 2 次**；
仍为 False → **停，如实报，不进入定稿+自测**。

**为什么敢重探**（裁定的理由）：拿死胡同账本去定稿+自测是必然白跑（第九轮实测 30 分钟全废）；
重探便宜、有界、非破坏（一趟 ~150 秒）；而且**它本身就是「能不能避开那条死路」的解法** ——
已量出「匹配不上」与答案相关而非必然（一支成功一支失败），换一组随机答案就可能走通，
于是不必先知道「哪个答案触发它」。**代价控制器认了**：若那条路是地区/邮编决定的，
最多白花 2 趟探索然后停下如实报 —— 不产出假成功、不无限重试。

**实现**：`agent/graph.py` 的 `_explore` 节点 —— 原来只记一句（上一轮），现在循环
（`EXPLORE_ATTEMPTS = 3` = 第一次 + 最多 2 次重探）；`False` 时
`end_reason = explore_unfinished` 并**直接返回**（不进 draft/selftest）。
顺带把**两次账本的差异**记下来（`explore_attempts` + `_attempts_note` 一句人话：
每趟「见到了成功文案没有 / 几步 / 停止原因 / 填过什么」）—— 那就是问题 2/3 的对照，
**控制器说不用再单独做实验**，重探那一趟本身就是对照。

**钉子 2 条**：① 三趟都见不到 → 探 3 趟后停、`end_note` 里写「重探了 3 趟」、
且 `rec.selftest` 为空（没进定稿+自测）；② 第 2 趟见到 → 用第 2 趟的账本往下走
（`rec.selftest` 非空）且差异写进 notes。替身 `_deps` 加了 `explores=[…]`（一串 journey）。

## 2. 没做到的：完整三遍

这一轮起跑了一次（`job-a6feb03adb22`：新窗口、闸口有人回话、没有看门狗），
**没跑完就被用户叫停**（「别重试了，可以了，资料不准先算了」）—— 于是：

- **`delivered=false`、`forms/sites/` 仍只有 `.gitkeep`**；
- 验收判据（三遍都见到 `Good news - We've matched you! Your quote is on the way!`）
  **没有验到**；「重探成功」也不等于成功（控制器原话）。

## 3. 顾虑（交接用）

1. **判据没放宽一个字**：现在这条链上，产物侧已到过 38 步 / 0 没做成、"帧/地址/正文"
   接上了 CDP 侧；探索侧补了「走到成功文案没有 + 有界重探」这道闸。
   **「三遍都见成功文案」仍未被任何一次完整运行验证过。**
2. **若那条「匹配不上」是地区/邮编决定的**（重探也走不通）→ 那是**判据本身**要谈的事
   （例如「见到成功文案**或**见到明确的拒绝页」），得用户点头，**不许自己放宽**。
   重探的差异记录已经就位（`explore_attempts`），一旦再跑就能直接读到。
3. 用户这一轮中途表示「资料不准先算了」—— 我按他的意思停了；再往下推需要他重新给信号。
