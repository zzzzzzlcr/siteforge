# 修复报告：读页面用的帧必须是**当场活着的那几个** + 对照实验 + 一条判据装饰字符

**状态**：DONE_WITH_CONCERNS —— 对照实验做了、三处修了、都有钉子；**自测仍未过**（差在哪见 §5）
**commit**：见文末（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **402 passed / 12 skipped**（新增 3 条钉子；旧断言一条没放宽）

## 1. 对照实验（第一件，顺序按控制器要求：先证明读得到）

**① 外部对照（同一窗口、同一时刻，从外面读那个活着的问卷帧）** —— 探针
`/tmp/gwacc4/liveframeprobe.py`（1.5 秒一次，原样记录 `/tmp/gwacc4/liveframeprobe.jsonl`）：

```
06:24:09 | 帧 2F9C2E6560 | Progress: 80%\n\nThis is the last page of questions.\nPhone Number:\n \nSee My Match
06:28:54 | 帧 133B33046A | Progress: 0%\n\nWhat is the year, make & model of your vehicle?\n…
```

**结论：那一刻那段文本读得到**（题目正文原样读出来了）—— 所以「读不到」是**产物自己的事** ✅

**② 产物手里那四样**（失败那一刻，靠这一轮新加的 `why` 自证）+ **对照**：

```
正文里没有「Progress: 30% What state do you live in?」；
页面上**实际看到的**开头那段是：「___ The listings featured are compensated and this influences…」
读页面用的帧：账本 ['A71C4F554D00083E183…'] ／ 活帧 […]
```

**差在两处，都是产物的**：
1. **活帧表被广告帧占着**：`live_frames` 原先**只增不减** —— 页面上的广告帧
   （`id-msp.newsbreak.com` 那种）一旦进来就永远在表里，它们活着 →
   `_live_frames_ok()` 永远为真 → **再也不会去找那个真正的问卷帧** →
   读页面只剩主帧 + 广告帧，问卷正文（与成功文案）**永远读不到** ✅
   （这正是控制器那条强线索的形状，旁证也对得上：几十趟自测**没有一趟报过成功**。）
2. **判据里带着装饰字符**：账本学到的判据是「`___ The listings featured are compensated and…`」——
   开头那个 `___` 是**广告位占位符**，第二趟它没了 → 页面上是「`The listings featured…`」→
   判据对不上 → 整组跳过，而**两句话明明是同一句** ✅

## 2. 修的三处

- **`_read_frames()`：读之前先确认活帧还活着**（`_live_frames_ok()`：读一眼就知道），
  确认不了就去找一次（`_refresh_live_frames()`，`observe` 一次，节流 `LIVE_PROBE_EVERY=3s`）。
  **老写法的套**：找活帧那一下挂在一个**不会被调用**的判据上 ——
  `_frames_all_dead()` 依赖 `_dead`，而 `_dead` 只有 `_urls()` 会填；
  判据（`when`）是**纯文本**的时候根本不走 `_urls()` → 探测从来不触发。
  → **判据**（控制器给的）：「读页面用的帧，是**当场活着的那几个**」。
- **`live_frames` 改成「最近一次观测里的帧」**（替换，不是累加）——
  见 §1 第 1 条。这一条是**这轮真正解开套的那一下**。
- **判据里那段正文开头不带装饰字符**（`_snippet` 削掉 `_ — - · * |` 与空白）——
  见 §1 第 2 条。**取判据时**削，判据本身仍然是从页面原文里取的子串。

钉子 3 条：`live_frames` 跟着最近一次观测走（累加会让广告帧挡住问卷帧）／
`_snippet` 不带开头装饰且不误伤正常正文／（外加）`why` 自证里带上「读页面用的帧」。

## 3. 第二件：窗口寿命（运维约束）

窗口只活 25~33 分钟，而「探路 + 三遍自测」贴到上限。**我的判断**：
- 自测那一步**本来就会换一个干净窗口**（R-F1 的 `fresh_session`：关旧窗 → 开新窗）——
  也就是说**自测开始那一刻窗口年龄归零** ✅ 这一条已经在生产路径上了；
- 真正把窗口耗光的是**停在闸口等人**（这一轮有一趟在 intake 闸上停了 25 分钟 → 窗口自然死亡，
  服务正确地拒绝继续）。所以这一件的处置是**运维侧**：闸口要有人（或自动）及时回话；
  真丢了窗就走 `POST /job/{id}/reopen` 续跑（那条路是现成的，账本在 checkpoint 里）。
- **不建议**把三遍自测拆开跑：它们共用一次会话正是第 2 遍（状态残留）要打的东西。

## 4. 真站读数（这一轮）

| 趟 | baseline | rerun | delay |
|---|---|---|---|
| `…062956` | 执行 1 / 跳过 35 / 没做成 1 | 8 / 24 / 5 | 3 / 33 / 1 |
| `…065021` | 0 / 10 / 0（**那一下窗口是 `chrome-error://chromewebdata/`** ✗）| 7 / 0 / 2 | — |

## 5. 没做到的（如实说）

**自测仍未过**：三遍都**没有见到成功文案** → `delivered=false`、`py_path=null`，
`forms/sites/` 里仍然只有 `.gitkeep`。**「执行 N 步」「0 没做成」都不算成功。**
这一轮的修法（活帧取当场 + 判据不带装饰）**都落在「跳过」那一族上**，
而跳过的直接原因每一趟都不一样（有的是 `chrome-error://`、有的是装饰字符、
有的是子帧地址）—— 也就是说：**这三处修完，我仍然没有在真站上验到成功文案**。
下一轮该做的（我建议）：把这些修法合起来跑一趟**不被打扰的**三遍
（闸口及时回话、窗口中途别丢），再看第一步跳过的 `why` 原文。
