# 修复报告（第九轮）：把「帧」整个搬到 CDP 那一侧（三处一起改）+ 里程碑**过了**

**状态**：DONE_WITH_CONCERNS —— 三处一起改完、钉子绿、**里程碑在真站上验过（过了 ✓）**；
**三遍验收仍未过**（见 §4：走得最深的几趟 31~38 步、0 没做成，但成功文案没出现）
**commit**：见文末（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **402 passed / 12 skipped**（旧断言一条没放宽）

## 1. 里程碑那个答案：**有** ✓（这一轮的判据过了）

八个字：`page_signature()` 里**现在有问卷正文了** ✓。三个探针（真站、活窗口、各一次）：

| 探针 | `when` 要什么 | 结果 |
|---|---|---|
| 合起来 | 帧自己的地址 **且** 问卷正文（`What is the year, make & model of your vehicle?`） | **没被跳过** ✓（两样都成立才可能不跳） |
| 只地址 | 只要求 `chameleon-na…/forms/7878`（**帧自己的地址**） | **没被跳过** ✓ → 地址那一路读到了 |
| 只正文 | 只要求问卷正文那一句 | **没被跳过** ✓ → 正文那一路读到了 |

⚠️ 一处要说清（不然读 trace 的人会以为没有）：trace 里的 `page_sig` **只留 600 字**，
而拼起来的正文里**主帧那段在前**（落地页推广文案）→ 问卷那几句落在 600 之后，
**打印出来的那 600 字里看不到它**。判据用的是**整个** `page_signature()`（不裁），
所以「这一步没被跳过」就是「正文里有它」的证明。

## 2. 三处一起改（它们是同一个病的三种表现，不是三个补丁）

**病**：读页面用的帧，来路有三条（主帧 DOM 里的 `<iframe>` 的 `src` / 账本里的 frameID /
历史上累加过的帧表），**没有一条指向「当场活着、且装着这条流程那些元素」的帧** ——
而成功文案就在那个跨源子帧里。第八轮已经量到：主帧 DOM 里**根本没有那个问卷 `<iframe>`**
（只拿到广告帧），而且 `src ≠ 帧自己的 location.href`。

**改法（一个源头：当场那次 `observe` 的模型）**：

1. **读页面用的帧表 = 当场那次观测里的帧**（`_note_live_frames`：**替换**语义，
   不是累加 —— R-68 量到的「只增不减」就是这个病的第一个表现）；
2. **地址从帧自己的 `location.href` 取**（`cdp eval --frame-id`）——
   `_iframe_srcs()`（主帧里读 `src` 那条路）**整个删掉**（它从根上够不着问卷帧）；
3. **「该去找一次吗」的门槛改成「手上那份观测说不说得清这一页现在有哪些帧」**：
   - 手上那份观测**过期了**（换过页）→ 说不清 → 去找；
   - 声明里有帧、可一个活帧都没有 → 说不清 → 去找；
   - 有活帧、可都答不上（死号）→ 说不清 → 去找。
   ⚠️ **老的「某个帧还能读」当门槛是不行的**：广告帧永远满足它（这正是 R-68 的形状）。
   ⚠️ **只在「这条流程确实活在帧里」时去找**（`self.frames` 非空 = 账本里有帧号）——
   一律去找会把 §13 那条「生产重跑不 observe」破掉（`tests/` 有钉子钉着它）。
4. 两个帧表的自证诊断**两支都有**（地址支 + 正文支）—— 第八轮那条跳过走的是地址支，
   而当时诊断只加在正文支，所以「手里到底有哪些帧」打不出来。

钉子：`test_the_url_judgement_reads_the_frames_own_location`（帧自己的地址那一路）、
`test_live_frames_are_the_ones_in_the_latest_observation`（替换语义）、
`test_the_skip_reason_carries_what_we_actually_saw`（改为查「读页面用的帧」那句自证）。
那条基于主帧 `iframe.src` 的旧钉子**按设计删掉了**（那条路已经不存在）。

## 3. 方法论上的一点（写给后面的人）

这一轮**先验里程碑，再跑三遍** —— 前八轮全是「跑了很多步但看不见终点」，
因为「走得深」与「看得见终点」是两件事。里程碑三个探针各一次、几分钟，
比跑三遍便宜得多，而且**过一次就说明那堵墙真的拆了**。

## 4. 三遍的验收（跑起来了，见文末补记）

窗口新开、闸口由驱动脚本及时回话、**没有看门狗**（第八轮量到：看门狗会跟服务的
窗口管理打架 —— `POST /run` 那一下服务本来就会给探路换干净窗口）。

## 5. 三遍验收（原样读数）

这一轮几趟的 trace（每遍一个子目录）：

```
gowizard-20260917-074157  baseline  执行 38 | 跳过 17 | 没做成 0 | 成功文案: 没出现
                          delay     执行 31 | 跳过 22 | 没做成 2 | 成功文案: 没出现
                          rerun     执行  6 | 跳过  0 | 没做成 3 | 成功文案: 没出现
gowizard-20260917-073346  delay     执行 37 | 跳过 17 | 没做成 1 | 成功文案: 没出现
```

**结论（不粉饰）**：**没有一趟见到 `Good news - We've matched you! Your quote is on the way!`**
→ `delivered=false`、`py_path=null`，`forms/sites/` 里仍然只有 `.gitkeep`。
**「执行 38 步」「0 没做成」都不算成功。**

⚠️ 一处**读 trace 的坑，写下来免得误判**：trace 里的 `page_sig` **只留 600 字**，
而拼起来的正文里主帧那段在前 → 问卷那几句落在 600 之后。
所以「`page_sig` 里搜不到问卷正文」**不等于**「判据没读到它」 ——
**判据用的是整个 `page_signature()`（不裁）**；证据是那些**要求问卷正文才成立的状态
确实被应用了**（执行 31~38 步、`when` 里就写着问卷正文）。里程碑的三个探针（§1）
就是这么各证一半的。

## 6. 顾虑

1. **三遍没过**：这一轮把「帧从哪来」这一个源头改到了 CDP 那侧、里程碑也过了
   （读得到问卷正文了），但**成功文案一次都没出现**。走得最深的一趟 38 步 / 0 没做成，
   说明「动作都做成了」，缺的是**走到最后那一页** —— 下一轮该看的是
   「那 17 步跳过 + 后面停在哪、第一条跳过的 `why`（现在两支都会把帧表摆出来）」。
2. 窗口寿命仍是硬约束：这一趟 job 跑了 **1769 秒（约 30 分钟）**才被我停掉
   （它自己在 draft → lint → selftest → diagnose 的循环里转）——窗口 25~33 分钟，
   **三遍 + 重试会贴到上限**。运维口径已写在 §4/§3：闸口及时回话、别加看门狗（会打架）、
   真丢了窗走 `reopen`。
3. trace 的 `page_sig` 只留 600 字这件事，**对「用 trace 判里程碑」是个坑**
   （见 §5 那条 ⚠️）—— 下一轮若还用 trace 当判据，建议把 `page_sig` 落**尾部**若干字
   或另落一个 `page_sig_has` 字段（这轮没做，怕动到下游读 trace 的地方）。
