# 修复报告：`when` 不成立就静默跳过（量清了 + 让跳过出声）

**状态**：DONE_WITH_CONCERNS —— 跳过这一格**量清了**、**出声了**（钉子 3 条）；
跳过的直接原因也修了（子帧地址从主帧的 `iframe.src` 认）；**但自测仍未过**（差在哪见 §5，不粉饰）
**commit**：见文末（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **397 passed / 12 skipped**（新增 3 条钉子，旧断言一条没放宽）

## 1. 证据留在哪儿（这一轮没删）

| 东西 | 路径 |
|---|---|
| 产物候选（图的收尾会删它，我每 2 秒抄一份） | `/tmp/gwacc4/kept/gowizard.candidate.1.py`（119,307 字节；另存一份 `kept_artifact_gowizard_candidate.py`） |
| 那趟的账本 | `/tmp/gwacc4/runtime/explore/job-5d6a3780b1e5/attempt-1.jsonl`（64 步 / 28 个动作步 / **探索见到成功文案**） |
| 自测三遍的 trace | `/company/siteforge/runtime/selftest/gowizard-20260917-035047/` |
| 抓现场的探针原始记录 | `/tmp/gwacc4/zipwatch.jsonl`（每 2 秒一帧：输入框现状 + 每个框的身份 + `elementFromPoint` 命中） |

## 2. 第 13 步之后为什么全被 `when` 跳过 —— **量出来的四问四答**

（先给产物加了「跳过出声」，于是**它自己把原因说出来了** —— 下面第一条就是它的原话。）

1. **产物一共几步、第一条被跳过的是第几步、它的 `when` 原文**：
   某趟 24 步、第一条跳过是**第 14 步**，`why` 原文（trace 里那一行）：
   ```
   地址对不上：要含「https://chameleon-na.www.gowizard.com/forms/7878/default/gowizard」，
   现在是「https://www.gowizard.com/auto/?text=Get+Matched&touchpointId…」
   ```
   也就是说：那个状态的 `when.url_contains` 是**子帧的地址**，而产物手上**只有主帧的地址**。
2. **那一刻页面上是什么**：还在问卷里（主帧地址 `…/auto/?text=Get+Matched…`），
   子帧（chameleon 部件）活着 —— 判据要的地址就在**主帧那条 `<iframe>` 的 `src`** 上。
3. **账本顺序 vs 现场顺序**：**不是随机的** —— 那几趟重放的年/品牌/车型顺序
   （Year→Make→Model）与账本完全一致（trace 里 2020→Audi→A4 / 2020→Buick→Envision 逐题对上）。
   所以**线性重放这条前提在这一站成立**；跳过不是「题目随机」引起的。
4. **`progress` 在 iframe 里可不可信**：**不可信**（实测同一趟里第 3、4、13 步
   `prog=False` 而后面明明走通了）。它**不是** `when` 的判据（`when` 只用 URL + 正文），
   但它喂的是**原地打转那个早停**（`DIFF_JUDGES = click/goto`，连续 3 次 False 就收摊）——
   所以在 iframe 为主的站上，它**有可能**提前收摊。这条记在顾虑里。

## 3. 顺着现场修的两件

- **判据能看到子帧地址了**：`_urls()` 现在把**主帧里那些 `<iframe>` 的 `src`** 也读进来
  （跨源 iframe 的**内容**读不到，**地址**一直读得到；而且不用帧号）。
  ⚠️ 第一版写错了：先读 `getAttribute('src')`（**相对地址**）→ 一条都对不上；改成
  **先读 `fs[i].src`（属性会解析成绝对地址）** 才对上。这一格是 trace 里那句 `why` 指给我看的。
- **`page_signature()` 读不到正文时**用**最近一次 observe 的 `page_text`** 兜底
  （同一口径：observe 的 page_text 本来就是跨帧拼的）—— 「读不到」不该被读成「页面上没有那句话」。

## 4. 跳过必须出声（这一条不管上面量出什么都得做，已做 + 已钉）

- 每个被跳过的步：**日志**里说明哪条判据不成立（`_when_why`：地址对不上 / 正文里没有哪一句）
  + **trace 里落一行**（`skipped: true`，带 `state` 与 `why`）。
- 收尾那句：**「走完了 N 步」不再是结论** —— 现在说的是
  「**没走到成功**：N 步里真做了 X 步、被 when 判据跳过 Y 步（跳过最多的是「某状态」）；
  页面上**从头到尾没有出现过成功文案**（要认的那段：…）」。
- 钉子 3 条：跳过在 trace 里留痕（带 state 与 why，且**不许真做**）／收尾必须大声说「没到」
  并报出「真做了几步 / 跳过几步」／子帧地址能从主帧那条 `src` 认出来。
  ⚠️ 收尾那句话**只进日志、不进 trace**：trace 每行都是「一步」，多加一行没有 `ok` 的
  会把下游（按 `line["ok"]` 读的那几处）读崩 —— 这是加它时踩到的，写下来免得后人再踩。

## 5. 这一轮**没做到**的

**自测仍未过。** 最后一趟（`job-5d6a3780b1e5`）的 baseline：
**10 步全做成、0 步跳过、但页面上始终没有成功文案** → `delivered=false`、产物没落盘。
`forms/sites/` 里仍然只有 `.gitkeep`。

**差在哪（如实说，不硬凑）**：这一趟的**产物只有 10 步**（账本里有 **28 个动作步**），
走到第 10 步（Model 答完）就没了下一步 —— 也就是说**账本里后面的那些步没进产物**
（`_replay_step` 只留「做成了的」步；具体是哪一条规则把它们滤掉的、以及那 18 步在账本里
长什么样，**这一轮我没有钉死**）。跳过那条路已经清了（这趟 0 跳过 ✓），
所以卡点这次不在 `when` 上。

## 6. 顾虑

1. **产物的步数比账本的少**（10 vs 28）是这一轮新暴露的、**没钉死**的一格：账本 64 行里
   28 个动作步、探索也确实走到了成功文案，而产物只有 10 步 —— 差额去哪儿了要单独量。
2. **`progress` 在 iframe 里不可信**（实测），而它喂的早停（连续 3 次 `prog=False` → 收摊）
   在 iframe 为主的站上有能力**提前收摊** —— 建议下一轮要么让它别在帧内步上计数，
   要么把「判不出」与「没推进」分开（`progress: null` + `progress_why` 已经有了，
   计数的那个 `stalled` 只该认**明确的 False**）。
3. `_read_frames()` 那次「全死了才去找活帧」的 observe，在**帧号全漂**的站上每一步都要探一次
   （找不到就下一轮再找）—— 代价没量。
