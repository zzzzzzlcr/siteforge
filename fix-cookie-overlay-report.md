# 修复报告：cookie 同意弹层盖住答题区（A/B/C 三件）+ 「那一步在不在账本里」的定论

**状态**：DONE_WITH_CONCERNS —— A/B/C 三件都落地、都有钉子、真站验到了「走深 17 步、一步没失败」；
**但自测仍没过**（差在哪见 §6，如实说）
**commit**：`f89ee30`（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **392 passed / 12 skipped**（新增 6 条钉子；旧断言一条没放宽）

## 0. 先认一句：控制器把结论改对了，我上一轮说的「多半是时序」是错的

我上一轮在 §8.5 里写「最后一寸是时序，我没钉死」——**不对**。现场是：
干净会话里的 **cookie 同意弹层盖住答题区**（截图 `3-before/4-before/5-after` 都在），
第一步的点击**点到弹层上**、cdp 照样回 ok → 静默假成功 → 后面全塌。
我那三件（zip 语义、③ 的漂帧回退、R-F1）方向都是对的；R-F1 本身没放宽任何判据 ——
它只是**暴露**了这个真缺陷（脏会话里根本不会有弹层）。

## 1. 「那一步是账本里从来没有，还是账本有、定稿时被丢了？」——**从来没有**（不是丢的）

两趟对比（同一套管线，相隔 15 分钟）：

| 那趟 | 账本里有没有 cookie 步 | 证据 |
|---|---|---|
| `gowizard-20260917-010951`（01:09） | **有** | 产物 trace 第 2 步就是 `click "Accept Cookies"` —— 产物只可能是照账本渲染的 |
| 最后一趟（`job-30b40c84fd92`，01:24 之后） | **没有** | 现存的账本里 `onetrust` 命中 **0** 条；开头就是 `goto → Get Free Quote` |

**为什么两趟不一样**：不是定稿丢的，是**探索那一刻页面上就没有弹层** ——
010951 那趟的窗口是刚开的（干净），弹层在、探路把它点掉了；
后一趟我用的窗口**已经被前一趟的产物点过同意**（cookie 还在），弹层不再出现。
**所以这条链对「弹层在不在」是不确定的，而生产每单都必然有弹层** —— 这正是 B 要修的东西。

## 2. A（产物侧）：自带防御性的弹层处理

`agent/template.py` 新增 `_clear_obstructions()`：**开跑前**与**每次 `goto` 之后**各看一眼，
有同意类弹层就点掉、没有就跳过。

- **判据与 `cdp observe` 的 obstructions 同一套词汇**（文字 / id / class 里有没有
  `cookie|consent|gdpr|privacy`）—— 但**不用 observe**：生产那个 cdp **没有 observe**
  （§4.6 的能力边界），而弹层恰恰在生产每单都出现。所以走 `eval`（**读**，§5.2 允许），
  点仍然走 `cdp click`。
- 只做**有把握**的那一下：可见的同意类容器 + **带稳定 id** 的按钮 + 文字属于
  「接受/同意/关闭/拒绝」那一族；找不到就**什么都不做**（宁可不做，也不猜一条会点错的选择器）。
- ⚠️ 探针**不走**「按选择器一步查」那种写法（`tests/test_template.py` 的「动作全走 cdp」
  把它钉死了）—— 改用 `getElementsByTagName('*')` + 属性自检。

## 3. C（诚实性）：快路点击也接遮挡判据

`_covered_by(selector, frame)`：点之前读一次「目标中心点上站着谁」
（`elementFromPoint`；接受自己 / 自己的后代 / 自己的祖先，别的一律算盖着）。
盖着就**不点**（换下一个候选；全被盖着 = 这一步没做成）—— 于是
「点到弹层上、cdp 回 ok、被记成做成了」这条**静默假成功**就没了。
量不出来（元素不在 / 零尺寸 / 点不在视口里）返回空串 = **不加判断**，
由 cdp 自己的滚动与报错去说。

## 4. B（探索侧）：探路也换成干净会话

`agent/service.py` 新增 `_clean_window_for_explore(brief)`：`POST /run` 那一刻，
这个部署给得了 `fresh_open` 就**换一个干净窗口**（清 cookie/缓存）并把它写进 brief ——
探路与后面所有步骤都用它。换不了就照旧用调用方给的那个 + 日志里说清
（「条件更差，不是这一单不能跑」）。
**这是系统性的**：不止 cookie 一件事 —— 探索的条件必须与自测/生产一致，
否则账本学的路径在生产里不成立。

## 5. 钉子（tests/，这一轮允许动）

| 钉子 | 钉的是什么 |
|---|---|
| `test_the_product_dismisses_a_consent_overlay_before_it_starts` | 有弹层 → **第一步之前**就点掉 |
| `test_without_an_overlay_nothing_is_clicked_for_it` | 反例：没弹层 → 一下都不点 |
| `test_a_covered_element_is_not_clicked_and_does_not_report_ok` | 被盖着 → **不点**且**不算做成** |
| `test_an_uncovered_element_is_clicked_as_usual` | 反例：没盖着 → 照常点 |
| `test_the_explore_also_gets_a_clean_window` / `..._keeps_the_original_and_says_so` | B 用新窗口 / 换不了时不说假话 |

替身 common 加了两格（加法）：`consent`（同意弹层的答复）与 `cover`（遮挡判据的答复，
**只认带 `COVER|` 前缀**的答复 —— 别的形状一律不当成盖着）。

## 6. 真站复跑：走深了很多，但**自测仍没过**（差在哪，如实说）

窗口新开（`pids/alive` 起 PID 184 → 服务换干净会话之后 2308），job `job-327479f14053`。

**探路（有 B 之后）**：79 步、**真的走到了成功文案**（最后一条 observe 的正文就是
`Good news - We've matched you! Your quote is on the way!`），最后几步是
姓名 → Continue → 邮箱 → Continue → 电话 → **See My Match** ✓。

**自测 baseline 的 trace（17 步，一步没失败）**：

```
 1 goto   打开了 /auto-warranty/
 2 click  点了「Reject All」          ← **干净会话**里它真的在（B + R-F1 的效果）
 3 click  点了「Get Free Quote」
 4 click  点了「Sedan」
 5-13     三个组合框 + 选项（Audi / A3…）lvl 1~2（活帧里重试声明选择器那条路）
14 form   填好了「textField-173862」（州）
16 form   填好了「textField-173838」（**ZIP**）
17 click  点了「Continue」；**页面没有变化**
```

**然后是**：第 18 步起的状态全部**静默跳过**（它们的 `when` 不成立），跑完也没见到成功文案 →
`selftest_cap`、`delivered=false`（**产物没落盘**）。

**我对「为什么第 17 步没推进」的判断（有证据，但没钉死）**：

1. **最像的是线索 2（备用选择器的歧义）** —— 这一轮**没修**（控制器说那是另一回事）：
   ZIP 那组的候选人里，`input.MuiInputBase-input.MuiInputBase-inputAdornedStart` 这种
   **通用**选择器命中不止一个输入框，而 cdp 的宽松路径取文档序第一个 ——
   值可能落进了**另一个**框（与用户截图里那句 `Please enter a valid US zipcode.` 同一族现象）。
   ⚠️ **C 那条遮挡判据救不了它**：两个候选都没被盖着，它是「选错了元素」不是「点到盖上」。
2. **另一种可能**：那一刻页面上有一个 `chameleon-overlay`（我事后 observe 到了：
   `obstructions: [('overlay', 'button.chameleon-overlay-close…')]`）—— 那是**部件自己的**浮层，
   **A 故意不点它**（只点同意类；一般的浮层去点它同样是「点到别的东西」）。它盖住时
   C 会让那一步不点、如实记没做成 —— 但那一步（`Continue`）trace 里是 ok=True，
   说明它当时**没被盖着**，所以这条更像**后果**（没推进之后浮层才起来）而不是原因。
3. 要钉死得在**那一刻现场**看：ZIP 框里到底填了什么、`Continue` 之后页面上说了什么。
   这一轮**没做到**，不写成结论。

## 7. 顾虑

1. **线索 2（通用选择器的歧义）现在是拦路的那一个**，而且它是**通用**的：
   同一段 class 组合被多个字段组共用 → 第一条一挂就把值打进别的框。
   修它要定「候选落地时怎么确认它就是那个字段」（产物侧现在没有这个判据）——
   建议下一轮专门做它（它同时解释了用户截图里的红字）。
2. **A 只点同意类**（`cookie-banner`），部件自己的 `overlay` 不点 —— 这是**故意的**
   （一般的浮层可能是页面要人看的东西，去点它同样是「点到别的东西」）。
   要不要扩到「带 `dismiss_selector` 且唯一的所有 kind」，另裁。
3. A 的探针每次跑要**多一次 eval**（开跑 + 每次 goto 后各一次），C 的遮挡判据**每次
   快路点击前一次 eval** —— 重跑路径仍然不调模型、不截图、不落 trace，但比原先多几次
   读进程。这是「不静默假成功」的价钱，写在这里备查。
4. 真站那趟的**探索**是在干净会话里跑的（B），所以账本第一次**带着 cookie 那一步**
   （rerun/delay 在第 2 步失败就是证据：那一步在、但那时弹层已经点掉了）——
   这正是「不确定」被消掉的样子；后面几趟应当稳定。
