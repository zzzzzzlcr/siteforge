"""探路那条路的**经验库** —— 踩过的坑、验证过的做法，进 `_SYSTEM`（稳定层）给模型当参考。

## 为什么要有这个文件（用户 2026-09-23 提的）

用户原话：「我感觉是不是有必要把我们写的成功案例比如约会站的随机题目怎么处理啊之类的
整理成知识库，给生成脚本的AI参考」。

## 两条纪律（不遵守的话，这个文件会变成最坏的那种东西）

1. **每一条都得有出处** —— 哪一个站、哪一趟、什么形状。没有出处的「经验」是**猜测**，
   而猜测写进提示词就是在教模型编：这个项目从头到尾治的就是「看起来像有据」。
   出处写不出来的，就不许进这个文件。
2. **短** —— 它每一趟探路都要进上下文（§13：重跑要便宜）。一条 = 一两句命令式的话。
   这里不是文档区：长文放 `docs/`，放进来的是**能直接用的一条判断**。

## 进哪一层、不进哪一层（2026-09-23 量过）

- ✅ **进 `_SYSTEM`**（稳定层）：`tests/test_browser_agent.py` 对它是**子串**断言 ⇒ 追加安全。
- ❌ **不进简报**（`_brief`）：没计划那一版被 B4 那条钉子**逐字节**钉死
  （`tests/test_browser_agent.py:1778`）⇒ 往里加东西等于偷偷把自由模式的行为改了。
"""

#: 一条一条的经验。形状：`scene`（**怎么认出**是这种情况）、`do`（**怎么做**）、`from`（出处）。
#: ⚠️ `from` 不许空、不许写「经验」「常见」这种没有指认能力的字 —— 见模块 docstring 第 1 条。
LESSONS = (
    {
        "scene": "页面上有随机/会变的内容（随机题面、随机顺序、A/B 文案、价格、倒计时）",
        "do": "判据钉**不变量**，别钉会变的那串字：钉结构（进度、下一步按钮在不在）"
              "或钉**每趟都一样**的框架文字；钉了会变的内容 = 这个状态每趟都可能整组不成立。",
        "from": "gowizard 2026-09-20：免责声明 A/B 两版，钉了 A 的那条判据在 72 趟里有 3 趟不成立",
    },
    {
        "scene": "看见 `#__BVID__…` / 一长串十六进制 / 带时间戳的 id 这类**会变**的标识",
        "do": "不许拿它当身份。换**语义或关系**去定位：文字、`name`、`placeholder`、"
              "以及关系型选择器（例如 `form:has(input[name=email]) button[type=submit]`）。",
        "from": "用户 2026-09-23 指出；`tools/cdp` 的 observe 已按这个顺序出候选"
                "（id → name → data-* → placeholder → 关系型 → class → 位置路径）",
    },
    {
        "scene": "按钮/输入框外面包着 `<form>`，而表单里有带 `name`/`placeholder` 的字段",
        "do": "优先**关系型**选择器（`form:has(<那个字段>) …`）—— 它随表单走、不随"
              "class 名和位置走。",
        "from": "afrotech / vogue 2026-09-23 真站：footer 那个订阅按钮的首选从 8 跳位置路径"
                "变成 `form:has(input[placeholder=\"Enter e-mail address\"]) button[type=submit]`",
    },
    {
        "scene": "读页面正文时**正文像是空的**（明明页面上有字，`innerText` 却几乎为零）",
        "do": "别急着判「这一页不像」：目标站可能有 **web component** —— shadow root 里的"
              "正文不在 `body.innerText` 里，要逐 root 收。读不到正文时**说清是读不到**，"
              "不要把「没读到」当成「页面上没有」。",
        "from": "afrotech 2026-09-23：`ShadowRoot` 上没有 `getElementsByTagName` ⇒ 每趟抛"
                "`TypeError` ⇒ 正文读成空 ⇒ 判据全不成立 ⇒ 7 步全跳（真站复现 20/20）",
    },
)


def block() -> str:
    """这一库经验拼成进 `_SYSTEM` 的那一段。**空库 ⇒ 空串**（一个字节都不多）。

    ⚠️ 措辞是**命令式**（与 `_hints_block` 同一条规矩）：进这里的是**规矩**，不是背景资料。
    """
    if not LESSONS:
        return ""
    lines = []
    for one in LESSONS:
        lines.append("- **%s** ⇒ %s\n  （出处：%s）" % (one["scene"], one["do"], one["from"]))
    return ("\n\n**别人踩过的（同一个形状就照这条来；每条都标了出处，别当成猜测）**：\n"
            + "\n".join(lines))
