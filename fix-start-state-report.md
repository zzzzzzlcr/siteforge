# 修复报告（第十一轮）：换窗之后起点状态要重认 + 「走到成功文案没有」这道闸原先不存在

**状态**：DONE_WITH_CONCERNS —— 两件都做了、三处钉子都扎了；**三遍验收仍未过**（见 §3，不粉饰）
**commit**：见文末（只本地 commit，没 push）
**一行测试结论**：`pytest tests/ -q` → **405 passed / 12 skipped**（新增 3 条钉子；旧断言一条没放宽）

## 1. 第一件（必修）：起点那一页是**旁枝**时，不给它设判据

**病**（上一轮量到的形状）：探索时浏览器停在 **Bit 的工作台页**
（`console.bitbrowser.net/…?id=…&port=…`），账本第一个「起点状态」的 `when` 于是要的是那一串 ——
而 `?id=…&port=…` **每开一次窗口都不一样**。自测换的是**干净窗口**（R-F1）→ 判据不成立 →
起点那组的 `goto` **一步都没轮到** → 后面全部静默跳过（实测 **0 执行 / 29 跳过**）。

**修法**（`agent/browser_agent.py`）：整趟走完之后做一次后处理
（`_drop_incidental_start_when`）：**起点那一页的主机名与后面每一页都不同** → 它是旁枝 →
把它的 `when` 撤掉，并在 `notes` 里说**人话**：

```
起点那一页（console.bitbrowser.net）与后面**每一页**都不同源 —— 它是「我们碰巧从那儿开始」
的旁枝，不是站点自己的页。所以它的状态**不设判据**（那种页的地址每开一次窗口都不一样，
设了判据会在换窗之后把起点那组步骤整组跳过）。
```

**为什么是后处理**（而不是「第一页一律不设判据」）：判「是不是旁枝」需要知道
**后面那些页长什么样** —— 那要等整趟走完。而「第一页一律不设判据」会误伤
**从入口开跑的探索**（第一页就是站点自己的页，那条判据是有意义的）——
我先写成那样，`tests/` 里 7 条断言当场红，说明它确实误伤了（钉子在替我看着）。

## 2. 第二件：三个答案（**先查再动**，逐条给证据）

**问 1：流水线现在有没有这道闸（探索结束时检查「成功条件出现了没有」）？**
**没有。** 核的办法是把 `success_text` 在图里出现的每一处都列出来（`grep -n success_text agent/graph.py`）：

| 位置 | 拿它干什么 |
|---|---|
| `intake`（`:307`） | **必须有人给**，没给就停（不猜成功判据） |
| `draft`（`:382`/`:386`） | 进产物：渲染成 `SUCCESS_TEXTS` |
| `selftest` | 产物自己拿它判「到了没有」 |
| —— | **探索那一趟有没有见到它：一处都没有** ✗ |

探索那一趟结束时只看 `stop_reason`（`model_done` / `budget_*` / `paused`）。
→ **拿一条死胡同的账本去定稿 + 自测，必然白跑**（真站实测：第九轮有一趟就是这么白跑的）。

**问 2：「Sorry we are unable to match you」那条分支怎么触发的？**
- **同一组答案重复跑、稳定还是随机：我没量**（这一轮的时间用在了另外两件上）—— **如实说没做到**。
- 手上有的两条旁证：① 有一趟探索（`job-3780f953e34d`，64 步）用 **Texas / 78701（Austin）**
  一路走到了成功文案 ✓；② 另一趟（`job-040aa5cc8b11` 之前那趟，账本已删 ✗）走到了
  「Progress: 50% Sorry we are unable to match you」那条分支 ✗。
  → **不是「每次都这样」，也不像纯随机**：更像是**答案相关**（站方按州/邮编找供应商，
  某些组合找不到）。**具体是哪个答案触发的，这一轮没定**（定它要同一组答案跑两遍对照）。

**问 3：能不能避开？**
**不知道**（同一个原因：没量）。两种可能的处置先摆在这里，**我不自己选**：
- 若「答对某个组合就不会进」→ 那是**探索的策略问题**（重探一趟 / 换一组答案）；
- 若「站方就是会拒某些地区」→ 那**「三遍都见成功文案」这条验收判据本身**要重新谈
  （例如「见到成功文案**或**见到明确的拒绝页」）——
  **改判据得您或用户点头，我不许自己放宽**（这一轮我一个字没放宽）。

**我这一轮做的**（不拦、只如实记，因为「要不要因此重探」是策略）：
探索结束之后，`_explore_reached_success(journey, success_text)` 会回答三态
（**True** = 某一步 observe 的正文里含成功文案，与产物同一口径的子串比；
**False** = 看过页面、一次都没见着；**None** = 没给判据 / 一次 observe 都没有 → **不猜**），
`False` 时往 `notes` 与闸口 facts 里写一句人话（「这一趟探路**没有在页面上见到成功文案**……
要不要重探一趟，请人或控制器定」）。钉子两条（见过 → True 且不加提醒；没见过 → False 且大声说）。

## 3. 第三件：验收

见文末（三遍）。

## 3. 三遍验收：仍未过（原样读数）

最后一趟（`runtime/selftest/gowizard-20260917-083008/`）在我按用户要求收手时只跑到
baseline 的头两步（`执行 0 | 跳过 2`）——**没跑完，也没见到成功文案**。
**`forms/sites/` 里仍然只有 `.gitkeep`（这一轮没有交付）。**

**这一轮的量级对比**（要看清是「哪一层」在挡路）：
- **产物那一侧**这一轮之前已经到过 **38 步 / 0 没做成**（第九轮），
  帧/地址/正文三件事都接上了 CDP 侧（第九轮里程碑三个探针各证一半 ✓）；
- 但**探索那一侧**并不保证账本里有那条路（§2 问 1：没有任何一处检查过），
  上一轮那趟 0 执行 / 29 跳过的主因也正是**起点状态**（这一轮修了，见 §1）。
- 所以现在这一格的形状是：**两件都在往好的方向走，但「三遍都见成功文案」还没在一次
  完整的运行里出现过**。要判它到底能不能过，需要一趟**不被打断**的完整三遍
  （闸口及时回话、窗口别丢、中途不打断）—— 这一轮没跑完就收手了（用户要求）。

## 4. 顾虑

1. **第二件的问 2/问 3 我没量**（如实说）：同一组答案重复跑是稳还是随机、
   那条「匹配不上」分支能不能避开 —— 都要**同一组答案跑两遍对照**，这一轮没做。
   我手上只有两条旁证（一支 Texas/78701 走到成功 ✓；另一支走到「unable to match」✗），
   **不足以定论**。
2. **「站方就是会拒」那种可能性一旦成立，验收判据本身要重谈**（例如
   「见到成功文案**或**见到明确的拒绝页」）—— 那是您或用户点头的事，
   **我这一轮一个字没放宽**。
3. 起点状态改的是**后处理**（整趟走完才判「是不是旁枝」）—— 若一趟探索**只看了 1 页**，
   后处理不动它（`len(pages) < 2` → 直接返回），那种账本的起点判据照旧留着。
4. 用户这一轮中途问过「怎么还在试」—— 我按他的意思**立刻收手**了：
   不再起新的验收跑，把状态如实写在这里。
