# gowizard 攻坚 · PDCA 汇报

**日期**：2026-09-17　**项目**：siteforge（agent 自主走站 → 产出 py 脚本）
**结论**：**未交付** —— 卡点已挖到底，方向已定并开工

---

## P · 计划

**目标**：用 agent 驱动真浏览器把 gowizard 的问卷走通，产出可进生产重放的 `forms/sites/gowizard.py`。
这是「规则折叠走到尽头、改用 agent 产出脚本」这条新路的**第一个真实站点验收**。

**验收判据（从头到尾一个字没放宽）**：
真站扰动自测 **三遍都见到**成功文案 `Good news - We've matched you! Your quote is on the way!`，产物才落盘。
「执行 N 步」不算 · 「0 步没做成」不算 · 「里程碑过了」不算 · 「占了位」也不算。做不到就如实报差在哪。

**边界（全程未破）**：
- 只本地 commit，**绝不 push**
- 绝不共用生产 worker / bit_id；实验全程走 `:1081`，**没碰过生产的 `:1080`**
- 生产脚本只读参照，一个字未改

---

## D · 执行

**循环**：先在真站取现场 → 定位真因 → 修 → 加钉子 → 回真站复验。**禁止「多半是时机问题」这类猜测。**

**规模**：12 轮修复 · 22 个本地 commit · 73 趟真站自测 · 406 项测试通过（旧断言一条没放宽）

### 挖出来的缺陷

| 卡在哪 | 真因 | 性质 | 状态 |
|---|---|---|---|
| 第 5 步 | 同意弹层盖住答题区，产物没有任何一步去点掉它 | 产物 + 探索会话不一致 | 已修 |
| 第 17 步 | 邮编框被判成手机号（框是 `type=tel`，语义信号全空） | 字段语义 | 已修 |
| — | `cdp form` 对歧义选择器**静默挑文档序第一个**（`click` 早修了，`form` 一直没修） | 对称性缺口 | 已修 |
| 第 13 步 | 状态判据不成立就**整组静默跳过**，跑完只有一句「走完了 N 步」 | 诚实性 | 已修 |
| 第 11 步 | 产物抛异常但 trace 一个字不落 → **「产物死了」被读成「走完了」** | 诚实性 | 已修 |
| 第 25 步 | 判据要的地址是子帧自己的 `location.href`，手里只有主帧里的 `src` | 帧 | 已修 |
| 结构性 | **主帧 DOM 里根本没有那个问卷 iframe** —— 而成功文案就在帧里，产物**结构上不可能报成功** | 帧（最根本） | 已修 |
| 结构性 | 「活帧」表只增不减 → 广告帧永久占位 → **再也不去找真正的问卷帧** | 帧 | 已修 |
| 起点 | 换干净窗口后起点状态不认 → **0 执行 / 29 跳过** | 回归 | 已修 |
| 流水线 | **账本里根本没有成功那条路**（探索走了「匹配不上」死胡同），而定稿前没人检查这件事 | 流水线缺口 | 已修 |
| 字段身份 | `observe` 收了 placeholder，**摘成「目标」时丢掉了** | 字段身份 | **处理中** |

**一个反复出现的形状**：系统把「没做成」说成「做完了」。十二条里有四条是这一族
（静默挑错元素 / 静默跳过 / 异常不落 trace / 静默填错值）。

---

## C · 检查

**达成的**：**产物第一次在真站上跑通** —— `runtime/selftest/gowizard-20260917-083953/baseline`
25 步全部执行、0 步没做成，**成功文案在第 25 步真的出现**。十二轮里唯一一次。

**没达成的**：判据要三遍全过，**只过了一遍**。挂掉的两遍原因**各不相同**：
- **第 2 遍**（产物缺陷）：账本里「点掉弹层」那一步是**硬步**，而这一遍 cookie 已被接受、横幅不在 → 失败。
  *修法明确：有就点、没有就跳过。*
- **第 3 遍**（环境）：打不开网址。按判据也不许当「过了」。

**根因结论**：十二轮之后才看清 ——
**这个站不是「字段没有名字」，是「名字摆在面前没伸手」。**
站方自己的配置是**公开可取**的（`curl` 就有，不需要窗口），每一格的身份**三层都有**。
siteforge 拿不到，是因为它**只认「位置路径」这一种地址**。
代价的形状很一致：位置路径会烂（帧号漂、页面重排、`nth-of-type` 失效）——
**这十二轮修的东西，几乎全是它的代价。**

---

## A · 处理

**方向已定并已开工**：把字段识别放开 —— 认种类放开到人眼能看见的、代码里写死的；
找地址优先用固定属性，位置路径降为最后手段。

### 那三层身份（都能复核）

| 层 | 内容 | 实测 |
|---|---|---|
| ① 站方语义 schema | DOM 的 `name="form_components[173838]"` + 配置的 `subtype` / `mappedQuestionId` | `textField-173838` **就是** `component_173838` —— 号直接对得上 |
| ② placeholder 原文 | `e.g. 06801`（→ 邮编）· `e.g. California or Texas`（→ 州）· `e.g. John Doe` · `e.g. example@email.com` · `e.g. (512) 494-9400` | 全齐，页面上肉眼可见 |
| ③ 题目正文 | `What state do you live in?` · `What's your ZIP code?` | 页面上就写着 |

**③ 正好补 ① 的缺口**：州那一格 `subtype` 和 `mappedQuestionId` **都是空的**，但题目正文白纸黑字。

### 为什么这不是拍脑袋

生产两套执行器**早就是这么干的**：
- **JSON 那条线**写着「**placeholder 必须原样保留（最高优先级！）**」
- **py 那条线**每轮**现读** `id | placeholder | name`，按关键词认种类，**认不出就不填**
- 共同点：**地址从不落盘，认的是语义**

**这不是发明新办法，是把生产已验证的办法，接到「字段没有名字」这一档上。**

### 下一步

1. 先过**中间里程碑**：真站上证明「读得到 placeholder / name」+「认得对那一格」，**再**跑三遍
2. 顺带修掉那个硬步（有就点、没有就跳过）
3. 验收判据**一个字不放宽**：三遍都见成功文案

---

## 证据

- **恢复地图（账本 R-55 → R-81，每条裁定都带「错了会怎样」）**：
  `.superpowers/sdd/2026-09-17-production-loop/progress.md`
- 十二轮报告：同目录 + `/company/siteforge/*-report.md`
- 历次自测 trace 与截图：`runtime/selftest/gowizard-*/`
- **22 个 commit 全部只在本地，未 push**
